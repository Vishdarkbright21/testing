#include <stdio.h>
int main()
{
    int source[50], dest[50];
    int i, size;
    
    printf("\nEnter the size of array: ");
    scanf("%d,&size");
     printf("\nEnter elements of source array: ");
     for(i=0;i<size;i++)
     {
         scanf("%d",&size);
     }
 
    return 0;
}